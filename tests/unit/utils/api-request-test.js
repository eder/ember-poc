import apiRequest from 'ember-poc/utils/api-request';
import { module, test } from 'qunit';

module('Unit | Utility | api request');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = apiRequest();
  assert.ok(result);
});
